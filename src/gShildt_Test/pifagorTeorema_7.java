package gShildt_Test;
// В этом примере демонстрируется динамическая инициализация переменных
public class pifagorTeorema_7 {
    public static void main(String[] args) {
        double a = 3.0, b = 4.0;
        double k = Math.sqrt(a * a + b * b); // Динамическая инициализации переменной
        System.out.println("Гипотенуза равна " + k);
    }
}
